mongod --bind_ip=$IP --dbpath=db --nojournal --rest "$@" 

if database is corrected -- before the @sign - between rest & "  -- add --repair, run once and then delete --repair & save - should work afterward


